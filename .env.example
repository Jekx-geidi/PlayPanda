# Copy this file to .env.local and fill in real values. .env.local is gitignored.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only — never reference from src/ (no VITE_ prefix, so Vite won't bundle it)
SUPABASE_SECRET_KEY=
